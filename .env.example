# CausalArmor Configuration
# Copy this file to .env and fill in the values.

# ---- CausalArmor Pipeline ----
CAUSAL_ARMOR_MARGIN_TAU=0.0
CAUSAL_ARMOR_MAX_LOO_BATCH=0
# Set to 0 for no limit, or a positive integer to cap concurrent LOO calls

# ---- Model configuration (shared across all providers) ----
# These env vars are picked up by all provider implementations.
# Explicit constructor args always take precedence over env vars.

# Proxy model for LOO log-prob scoring (VLLMProxyProvider, LiteLLMProxyProvider)
CAUSAL_ARMOR_PROXY_MODEL=google/gemma-3-12b-it

# vLLM server URL (VLLMProxyProvider only)
CAUSAL_ARMOR_PROXY_BASE_URL=http://localhost:8000

# Action regeneration model (all ActionProvider implementations)
CAUSAL_ARMOR_ACTION_MODEL=gpt-4o

# Sanitizer model (all SanitizerProvider implementations)
CAUSAL_ARMOR_SANITIZER_MODEL=gpt-4o-mini

# ---- API Keys (set for your chosen provider) ----
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
